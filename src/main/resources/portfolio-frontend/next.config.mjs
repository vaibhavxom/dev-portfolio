/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/dev-portfolio',        // Replace with your repo name
  assetPrefix: '/dev-portfolio/',    // Replace with your repo name
};

export default nextConfig;